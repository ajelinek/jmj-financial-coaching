---
import styles from './TestimonialsSection.module.css'
import QuoteIcon from './icons/QuoteIcon.astro'
import StarIcon from './icons/StarIcon.astro'

const testimonials = [
  {
    quote: "When our phone call started, I was so stressed I couldn't breathe. Now I am giddy with possibility!",
    author: 'Client',
  },
  {
    quote:
      'JMJ Financial has helped our family tremendously... I highly recommend you sign up with JMJ. You will not regret it!!!',
    author: 'Client',
  },
  {
    quote: 'Jennifer is super hands-on, walks you through everything, and encourages you when you feel overwhelmed.',
    author: 'Client',
  },
]
---

<section class={styles.testimonialsSection} aria-labelledby='testimonials-heading'>
  <div class={styles.testimonialsContainer}>
    <div class={styles.testimonialsHeader}>
      <div class={styles.headerIconWrapper}>
        <QuoteIcon width='64' height='64' class={styles.headerSectionIcon} />
      </div>
      <h2 id='testimonials-heading' class={styles.testimonialsHeaderH2}>What My Clients Say</h2>
    </div>
    <div class={styles.testimonialsGrid}>
      {
        testimonials.map(testimonial => (
          <blockquote class={styles.testimonialCard}>
            <div class={styles.quoteIcon}>
              <QuoteIcon width='24' height='24' />
            </div>
            <p class={styles.testimonialQuote}>{testimonial.quote}</p>
            <footer class={styles.testimonialAuthor}>
              <div class={styles.starRating}>
                <StarIcon width='16' height='16' />
                <StarIcon width='16' height='16' />
                <StarIcon width='16' height='16' />
                <StarIcon width='16' height='16' />
                <StarIcon width='16' height='16' />
              </div>
              <span>— {testimonial.author}</span>
            </footer>
          </blockquote>
        ))
      }
    </div>
  </div>
</section>
